# RealFeel Temperature

Install or refresh the RealFeel Temperature custom integration inside Home Assistant.

This add-on exists so `https://github.com/parnunu/home-assistant-addons` can stay your single Home Assistant repository URL even though the source project is a custom integration repository rather than a native add-on repository.
