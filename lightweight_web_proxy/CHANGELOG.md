# Changelog

## 1.0.0

- Initial release.
- Add nginx reverse proxy with Home Assistant ingress support.
- Add configurable target URL, Host header, TLS verification, timeouts, body size, and log level.
